[build]
  command = "pnpm install --no-frozen-lockfile && pnpm run build:client"
  functions = "netlify/functions"
  publish = "dist/spa"


[functions]
  external_node_modules = ["express"]
  node_bundler = "esbuild"
  
# API redirect removed - using localhost:3001 backend server
# [[redirects]]
#   from = "/api/*"
#   to = "http://13.51.196.113:3000/api/:splat"
#   status = 200
#   force = true
# SPA Fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
